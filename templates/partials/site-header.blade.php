<header class="main">
    <h1 class="site-title">
        <a href="{{ home_url() }}">{{ get_bloginfo('name') }}</a>
    </h1>
    @include('partials.edition-info')
</header>

@include('partials.banner')